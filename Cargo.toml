[workspace]
members = [
    "ceramic-config",
    "cli",
]

[profile.release]
# Tell `rustc` to optimize for small code size.
opt-level = "s"

# generated by 'cargo dist init'
[profile.dist]
inherits = "release"
debug = true
split-debuginfo = "packed"

